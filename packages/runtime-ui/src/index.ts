import Button from './button';
import Container from './container';
import Page from './page';
import Text from './text';

const ui = {
  page: Page,
  text: Text,
  container: Container,
  button: Button,
};

export default ui;
